# Leave empty when the UI is served from the same origin as the API (Railway all-in-one or nginx compose).
# For local Vite dev against a remote API, set the absolute API origin, e.g. https://api.example.com
VITE_API_URL=
VITE_SOCKET_URL=
